lyrics <- function(first, last) {
  
}

verse <- function(number) {
  sprintf("%s bottles of beer on the wall, %s bottles of beer.\nTake one down and pass it around, %s bottles of beer on the wall.\n", number, number, number - 1)
}
